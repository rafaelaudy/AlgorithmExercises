const { Graph, Node } = require("../structures/graph-my-take");

const sortedArrayToBinaryTree = sortedArray => {};

const { assert } = require("chai");

describe("Sorted array to binary tree", () => {
  it("Should have the minimum height", () => {});
});
